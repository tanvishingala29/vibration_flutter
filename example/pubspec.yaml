name: vibration_flutter_example
description: Demonstrates how to use the vibration_flutter package.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  vibration_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
